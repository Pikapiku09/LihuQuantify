# 决策日志

> 用途：统计纪律管理。每行记录"日期 / 基于哪段数据 / 改了哪条规则 / 该段数据此后永久失去验收资格"。
> 铁则：每次改规则前先翻本日志，确认新规则的数据段未被消费过。
> 任何一段数据一旦用于做决策，永久不能再作为验收依据（防数据窥探）。

| 日期 | 基于哪段数据 | 决策/规则变更 | 数据段状态 |
|---|---|---|---|
| 2026-08-26 | 50只主板×2年（2024-07~2026-08）前2/3 训练段（2024-07~2025-12） | 网格搜索选定参数：close_to_ma5_max_dev=0.05, freshness=3, chasing=0.08 | 训练段已消费，永失验收资格 |
| 2026-08-26 | 同上后1/3 验证段（2025-12~2026-08）+ 同段分段统计（震荡段胜率41.9%） | 推导并启用"市场状态过滤：仅上涨段（000001.SH 20日涨幅≥3%）开新仓" | 验证段已被用于调规则，**永失验收资格**（第二轮修复A认定的数据窥探） |
| 2026-08-26 | 无（设计决策） | 第二轮修复清单 A/B/C/D/E/F/G/H 实施 | 不涉及数据消费 |
| 2026-08-26 | holdout 段 2023-01~2024-06（首次使用，仅验证 ON/OFF 不调参） | 市场过滤 holdout 验证：**ON(+7.21%) vs OFF(-7.52%) 有效，但对阈值/窗口脆弱**（3%-20d:+7.2% / 2%-20d:+0.5% / 4%-20d:+7.7% / 3%-60d:-8.1%，极差15.8%>10%）。按修复A验收标准：**降级为"降仓信号"——非上涨段仓位减半，不再完全禁止开仓** | holdout 段已消费，永失验收资格 |
| 2026-08-26 | 训练段 2024-07~2025-12（再次观察，性质=稳健性诊断，非调参） | **修复I/J 诊断**：①扩边网格（ma5 0.03-0.10×fresh 2-5×chase 0.06-0.10）在旧池 old50 上：原参数(0.05,3,0.08)不稳健（收益+59.8%，邻域-2.37%~+83.2%含负），最优继续外移至新边界0.10（+89.6%）→ 边界伪影证实；②换分层池 strat200（成交额5层等比抽样）：**36/36 组合全稳健**，原参数 +244.8%（邻域+42.5%~+444.8% 全正）→ **池子偏差证实**（head-50 深市老牌股严重低估策略）。**决策：巡检池切换为 strat 模式（settings universe.pool_mode: strat），参数保持 (0.05,3,0.08) 不变（诊断不选参；最优点仍在边界0.10，单点不可靠，维持稳健区域内）** | 训练段第二次使用（诊断性）；注意 strat200 的 +244.8% 为同段重观察值，**不构成真实期望收益证据**，真实表现须由未来 live 纸面积累验证 |
| 2026-08-26 | 训练段 2024-07~2025-12 + holdout 段 2023-01~2024-06（第三轮重跑，性质=口径对齐诊断，非调参） | **修复A(第三轮)重跑（含连亏3笔停手+板块40%口径）**：①strat200 池：原参数 +244.8%→**+89.74%**（停手规则削去 ~155pp），邻域 32.79%~338.64% 全正，**仍稳健**，稳健点 36/36 不变；②old50 池：原参数 +59.8%→+33.16%，邻域 -9.36%~+46.28% 含负，**仍不稳健**，稳健点 24/36→16/36；③市场过滤 holdout 重跑（reduce 模式+停手口径）：ON-3%-20d **-0.12%** vs OFF **-7.53%**（过滤仍+7.4pp 保护，但 reduce 模式自身不盈利；对照第二轮 block 模式 +7.21%），变体极差 8.23%（较二轮 15.8% 收窄但 60d 窗仍 -8.04% 负）→ **维持"脆弱"判定与 reduce 起步**。**决策：无参数变更；(0.05,3,0.08) 与 reduce 模式维持** | 训练段第三次使用、holdout 第二次使用（均诊断性口径对齐）；新结论：停手+板块铁律使全网格收益系统性下移约 1/3~2/3，此前所有未含此二规则的数字均属乐观口径 |

## 可用数据段状态（截至 2026-08-26）

- 2024-07 ~ 2025-12：训练段，已消费 ×3（网格选参 + I/J 稳健性诊断 + 第三轮停手口径重跑）❌
- 2025-12 ~ 2026-08：验证段（已被用于调规则），已消费 ❌
- 2023-01 ~ 2024-06：holdout，已消费 ×2（修复A 首次 + 第三轮停手口径重跑）❌
- 2023-01 之前：未使用 ✅（数据充足时可用于未来决策）
- 2026-08 之后（未来）：live 纸面验证段，持续积累 ✅

> 维护规则：新增决策行时，同步更新上面的"可用数据段状态"。

## 市场过滤证据等级记录（修复G，第三轮）

- **证据等级：中等偏弱**——holdout block 模式 ON-3%-20d 仅 16 笔样本（+7.21% vs OFF -7.52%）；
  reduce 模式（当前生产配置）77 笔样本 -0.12% vs OFF -7.53%（有保护、不盈利）。
- **变体脆弱**：2% 阈值近乎无效、60 日窗为负。极差 8.23%~15.8% 均超 10%/接近阈值。
- **决策：采用 reduce 起步；积累 ≥100 笔纸面交易后再评估是否升级 block。**
  每月月度复盘自动统计过滤命中（`data/filter_stats.json` → 月度报告"过滤命中统计"栏目）。

## Live 积累期记录（2026-08-26 起）

| 日期 | 事件 | 说明 |
|---|---|---|
| 2026-08-26 | live 模拟盘启动 | 首次巡检（分层池50只）：69 信号 → 8 只成交（均同时挂止损 ✓）→ 61 拦截（资金不足 + 铁律1）。市场状态"震荡"→ reduce 半仓。初始资金 10 万，权益快照 100,753 |
| 2026-08-26 | **口径修正（live 首日发现）**：模拟盘"当日新仓不参与当日收盘止损判定" | 首日 4 只新仓被 MA10 破位即时标记（信号日收盘<MA10 但策略允许乖离MA5 -5% 入场）——回测中 T+1 开盘成交的仓首次判定在 T+1 收盘，模拟盘原逻辑在信号日当天判定 → 比回测苛刻，产生回测不存在的当日 churn。已修正对齐（buys_today 跳过），并清除旧逻辑产生的 4 条误标记 pending_stops。**此为口径对齐修复，非参数变更，不消耗数据段** |
| 2026-08-26 | **第四轮运维改造（部署与运维清单 11 项全落地）** | 不动策略/风控，只做可靠性：①邮件告警（EmailAlerter，SMTP SSL，warn/error 即时 + 每日摘要，配置空=零侵入）；②healthchecks.io 缺席心跳（start/success/fail ping，URL 空=no-op）；③巡检时点 15:30→16:30（Tushare 日线 16:00 后完整）；④Docker 化（Dockerfile+compose，TZ=Asia/Shanghai，DS918+ 部署）；⑤幂等保护（data/last_scan.json，当日重复巡检自动跳过，--force 强制重跑）；⑥loguru 文件日志（data/logs/，30 天滚动，scheduler/web 分文件）；⑦备份脚本（scripts/backup_data.py，zip+30 份滚动+恢复演练，被锁 duckdb 容忍跳过）；⑧安全边界（.gitignore 补 token/.env/data，web 默认 127.0.0.1）；⑨DEPLOY_NAS.md + DEPLOY_WINDOWS.md |
| 2026-08-26 | **数据层修复（清单3 延伸发现）**：index_daily 固定参数永久缓存导致"最新交易日"卡死 | `_market_state()`/`get_latest_trade_date()`/`build_stratified_pool()`/`data_manager` 四处 index_daily 查询用固定参数（end_date=20301231）+ 无 TTL 文件缓存 → 15:31 生成的缓存在 16:30 后仍命中，latest 永远停在昨日。修复：四处改 `use_cache=False` 直连（每日 1 次，符合 index_per_run=1 限频）。**22:45 验证：latest 从 08-25 推进到 08-26，当晚补跑基准日 08-26 巡检成功（67 信号，3 笔 MA10 破位登记，总资产 100,753）**。属数据管道修复，非策略变更 |

> Live 积累期纪律：不回头修改参数/池/过滤规则；观察指标与验收时间表见 USER_GUIDE §5.0。

## holdout 验证原始数据（2023-01~2024-06，50只）

| 变体 | 收益 | 回撤 | 卡玛 | 胜率 | 盈亏比 | 轮次 |
|---|---|---|---|---|---|---|
| OFF（不过滤） | -7.52% | -14.78% | -0.36 | 59.0% | 0.54 | 78 |
| ON-3%-20d | +7.21% | -2.11% | 2.37 | 75.0% | 1.01 | 16 |
| ON-2%-20d | +0.54% | -5.59% | 0.07 | 50.0% | 1.06 | 26 |
| ON-4%-20d | +7.69% | -1.01% | 5.28 | 76.9% | 1.27 | 13 |
| ON-3%-60d | -8.11% | -13.96% | -0.41 | 47.4% | 0.72 | 38 |

> 解读：20日窗口的三个阈值变体均显著优于 OFF；60日窗口失效——过滤依赖"近20日动量"，
> 换窗口结论翻转，属"方向敏感"。因此不作为铁律，作为降仓信号保留。
